---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-tyscript-demo
  labels:
    app: api-tyscript-demo
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  selector:
    matchLabels:
      app: api-tyscript-demo
  template:
    metadata:
      labels:
        app: api-tyscript-demo
    spec:
      containers:
      - name: primary
        image: ${image}
        ports:
        - containerPort: 3004
        resources:
          limits:
            memory: 100Mi
        readinessProbe:
          httpGet:
            path: /
            port: 3004
        livenessProbe:
          httpGet:
            path: /
            port: 3004
        envFrom:
        - configMapRef:
            name: api-tyscript-demo-config
      imagePullSecrets:
      - name: regcred-harbor
---

kind: Service
apiVersion: v1
metadata:
  name: api-tyscript-demo
  namespace: tyscript-dashboard  
spec:
  selector:
    app: api-tyscript-demo
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3004
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: api-tyscript-demo
  namespace: tyscript-dashboard  
spec:
  rules:
  - host: $(ING_NAME).tyscript-dashboard.arctic.true.th
    http:
      paths:
      - backend:
          serviceName: api-tyscript-demo
          servicePort: 80
